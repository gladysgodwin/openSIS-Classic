apiVersion: apps/v1
kind: Service
metadata:
  name: opensis_db
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/path: /metrics 
    prometheus.io/port:   '3307'
  namespace: opensis
  labels:
    name: opensis_db

spec:
  ports:
  - port: 3306
    targetPort: 3307
  selector:
    name: opensis_db  